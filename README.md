# DemoSelenium


Selenium framework under development

will be extending with pausing the pipeline- with polling a google excel sheet, reading data from db and different resources,(json, xml, csv, etc

when pushing to pipeline and main - tests are run

and if the tests fail then push to main is not permitted
